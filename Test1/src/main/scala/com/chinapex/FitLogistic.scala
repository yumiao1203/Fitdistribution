package com.chinapex
import breeze.linalg.{DenseVector => BDV}

/**
  * Created by josh on 17-5-26.
  */
object FitLogistic {
//  def main(args: Array[String]) {
//    val vec = BDV(0.1, 0.1, 1.0, 0.5, 0.5, 1.2, 0.3)
//    fitLogistic(vec)
//  }
//  def fitLogistic (vec: BDV[Double]): Unit = {
//    println(vec.size,mean(logVec),mean(vec))
//    val ss = Logistic.SufficientStatistic(vec.size,mean(logVec), mean(vec))
//    val parm = Gamma.mle(ss)
//    println(parm)
//  }
}
